#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 * @c: alphabet to be checked
 * Return: Always 0
 */
int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
